# Redis/Valkey 集群的"隐藏杀手"：硬盘故障如何击溃内存数据库

> 本文深入剖析 Redis/Valkey 集群在硬盘故障场景下的可用性问题，结合 Valkey 社区 PR #1032 和 PR #3372 的讨论，以及腾讯云 CRedis 的生产实践，揭示内存数据库中一个容易被忽视的设计缺陷，并提供完整的解决方案。

---

## 目录

- [一、引言：内存数据库真的不依赖硬盘吗？](#一引言内存数据库真的不依赖硬盘吗)
- [二、问题剖析：nodes.conf 的致命角色](#二问题剖析nodesconf-的致命角色)
- [三、灾难场景：一次硬盘故障引发的集群雪崩](#三灾难场景一次硬盘故障引发的集群雪崩)
- [四、社区的解决方案](#四社区的解决方案)
- [五、解决方案：从社区修复到工业级实践](#五解决方案从社区修复到工业级实践)
  - [5.1 社区解决方案：Valkey 9.1 的 cluster-persist-config 配置项](#51-社区解决方案valkey-91-的-cluster-persist-config-配置项)
  - [5.2 云上运维方案：腾讯云 CRedis 的实践](#52-云上运维方案腾讯云-credis-的实践)
  - [5.3 两种方案的对比与选择](#53-两种方案的对比与选择)
- [六、设计哲学：一致性与可用性的权衡](#六设计哲学一致性与可用性的权衡)
- [七、生产环境最佳实践](#七生产环境最佳实践)
- [八、总结与展望](#八总结与展望)

---

## 一、引言：内存数据库真的不依赖硬盘吗？

作为运维或开发人员，你可能会认为：

> **"既然 Redis 是内存数据库，那么即使禁用了 RDB 和 AOF 持久化，它也应该完全不依赖硬盘，对吧？"**

**答案是：错。**

很少有人知道，一次普通的**硬盘只读故障**，就足以让 Redis/Valkey 集群中的节点接连"自杀"退出——轻则单个节点下线触发故障转移，重则多个节点近乎同时退出，导致集群可用性严重受损甚至彻底不可用。更令人意外的是，这与你是否开启了 RDB 或 AOF 持久化**没有任何关系**——即使你关闭了所有持久化功能，这个问题依然存在。

这是一个广泛存在的**认知盲区**。大多数用户会认为：

| 用户认知 | 实际情况 |
|----------|----------|
| ✅ 禁用 RDB = 不写 dump.rdb | 正确 |
| ✅ 禁用 AOF = 不写 appendonly.aof | 正确 |
| ❌ 禁用持久化 = 完全不依赖硬盘 | **错误！** |

实际上，即使禁用了所有持久化功能，Redis/Valkey 集群模式下的 `nodes.conf` 写入**仍然是强制的**，而且是**同步阻塞**的。

2024 年 9 月，Valkey 社区开发者 enjoy-binbin 提交了一个 PR（[#1032](https://github.com/valkey-io/valkey/pull/1032)），历时 **15 个月**的讨论后于 2025 年 12 月合并。随后在 2026 年 3 月，为了平衡向后兼容性和灵活性，又提交了 PR [#3372](https://github.com/valkey-io/valkey/pull/3372)，引入了 `cluster-persist-config` 配置项，允许用户在一致性和可用性之间做出选择。这两个 PR 共同揭示并完整解决了这个问题。

本文将从**问题本质**、**社区讨论**、**工业级解决方案**三个维度，全面剖析这个问题。

---

## 二、问题剖析：nodes.conf 的致命角色

### 2.1 什么是 nodes.conf？

在 Redis/Valkey 集群模式下，每个节点都会维护一个名为 `nodes.conf` 的配置文件（通过 `cluster-config-file` 配置项指定），用于存储集群的元数据信息。

**`nodes.conf` 的格式与 `CLUSTER NODES` 命令的输出基本一致**，都是空格分隔的 CSV 格式，每一行代表集群中的一个节点。

以下是一个典型的 `nodes.conf` 文件示例：

```
5e5da837dc5f44e84973b7aa53165067b4ceba20 127.0.0.1:30001@40001,,tls-port=0,shard-id=5d2b0a860ae39f2068e7d373e02d706d62f9b79b master - 0 1721742292283 1 connected 0-5460
1e128f34081d0aad695a7431acd8de6d4fad1c00 127.0.0.1:30002@40002,,tls-port=0,shard-id=d02f78a588bfb896c7eae258bd82eeaacce1ab41 master - 0 1721742292384 2 connected 5461-10922
4054a107dc74a57784ed0067606f29559d9b988b 127.0.0.1:30003@40003,,tls-port=0,shard-id=faff28fe6480613fb062f07dde507bc3d0749c25 myself,master - 0 0 3 connected 10923-16383
81f1713ad9311fcc6ea6bfea0ab45da516f1a03d 127.0.0.1:30004@40004,,tls-port=0,shard-id=faff28fe6480613fb062f07dde507bc3d0749c25 slave 4054a107dc74a57784ed0067606f29559d9b988b 0 1721742292000 3 connected
64092da4a5e1c9d8d50d6bee7ca8100f6b27e281 127.0.0.1:30005@40005,,tls-port=0,shard-id=5d2b0a860ae39f2068e7d373e02d706d62f9b79b slave 5e5da837dc5f44e84973b7aa53165067b4ceba20 0 1721742292000 1 connected
4b40909a252dc14e770442578efe345539117287 127.0.0.1:30006@40006,,tls-port=0,shard-id=d02f78a588bfb896c7eae258bd82eeaacce1ab41 slave 1e128f34081d0aad695a7431acd8de6d4fad1c00 0 1721742292000 2 connected
vars currentEpoch 6 lastVoteEpoch 0
```

#### 行格式结构

每行的通用格式如下：

```
<id> <ip:port@cport[,hostname]> <flags> <master> <ping-sent> <pong-recv> <config-epoch> <link-state> <slot> ... <slot>
```

#### 字段详细说明

根据 [Redis 官方文档](https://redis.io/docs/latest/commands/cluster-nodes/)，各字段含义如下：

| 字段 | 说明 | 示例 |
|------|------|------|
| **id** | 节点 ID，40 字符的全局唯一字符串，创建时生成，除非执行 `CLUSTER RESET HARD` 否则永不改变 | `5e5da837dc5f44e8...` |
| **ip:port@cport** | 客户端连接地址（IP:端口）和集群总线端口（@cport），可选包含 hostname | `127.0.0.1:30001@40001` |
| **flags** | 逗号分隔的标志列表（详见下方标志位说明） | `myself,master` |
| **master** | 如果是从节点且已知主节点，显示主节点 ID；否则显示 `-` | `-` 或 `4054a107dc74a...` |
| **ping-sent** | 当前激活的 ping 发送时的 Unix 时间戳（毫秒），无待处理 ping 则为 0 | `0` |
| **pong-recv** | 最后一次收到 pong 的 Unix 时间戳（毫秒） | `1721742292283` |
| **config-epoch** | 当前节点（或其主节点）的配置纪元，故障转移时会生成新的、唯一的、单调递增的纪元 | `1` |
| **link-state** | 集群总线连接状态：`connected` 或 `disconnected` | `connected` |
| **slot** | 哈希槽编号或范围（从第 9 个参数开始，最多 16384 个条目） | `0-5460` 或 `3894` |

#### 标志位（flags）详解

| 标志 | 含义 |
|------|------|
| `myself` | 当前连接的节点本身 |
| `master` | 节点是主节点 |
| `slave` | 节点是从节点（为向后兼容保留此词） |
| `fail?` | 节点处于 `PFAIL` 状态（可能失败），对当前节点不可达但逻辑上仍可达 |
| `fail` | 节点处于 `FAIL` 状态，多个节点无法访问它，`PFAIL` 升级为 `FAIL` |
| `handshake` | 不可信节点，正在握手阶段 |
| `noaddr` | 没有该节点的已知地址 |
| `nofailover` | 从节点不会尝试进行故障转移 |
| `noflags` | 没有任何标志 |

#### 特殊槽位状态

在槽位迁移过程中，可能会出现特殊状态：

| 状态 | 格式 | 说明 |
|------|------|------|
| **Importing** | `[slot_number-<-importing_from_node_id]` | 槽位正在从其他节点导入中 |
| **Migrating** | `[slot_number->-migrating_to_node_id]` | 槽位正在迁移到其他节点 |

示例：
- `[93-<-292f8b365bb7edb5e285caf0b7e6ddc7265d2f4f]` - 正在从指定节点导入槽位 93
- `[77->-e7d1eecce10fd6bb5eb35b9f99a514335d9ba9ca]` - 正在将槽位 77 迁移到指定节点

#### 文件末尾的全局变量

`nodes.conf` 文件末尾还包含全局变量：

```
vars currentEpoch 6 lastVoteEpoch 0
```

- **currentEpoch**：当前集群的全局配置纪元
- **lastVoteEpoch**：该节点上次投票时的纪元

**`nodes.conf` 本质上充当了 Redis 集群元数据的"数据库"。** 当节点进程重启时，可以通过 `nodes.conf` 里的内容快速恢复自己视角里的集群拓扑。

### 2.2 nodes.conf 何时更新？

每当集群中发生以下元数据变更时，都需要将新的元数据写入 `nodes.conf`：

- 新增节点（CLUSTER MEET）
- 删除节点
- 节点角色变换（主从切换）
- 节点 config epoch 变化
- 节点负责的 slot 发生变化（槽位迁移）
- 节点投票（故障转移期间）

### 2.3 致命的设计：clusterSaveConfigOrDie

在 Redis 社区的原始设计中，存在一个名为 `clusterSaveConfigOrDie` 的函数。从名字就可以看出它的行为模式：**保存配置，否则死亡（die）**。

```c
void clusterSaveConfigOrDie(int do_fsync) {
    if (clusterSaveConfig(do_fsync) == C_ERR) {
        serverLog(LL_WARNING, "Fatal: can't update cluster config file.");
        exit(1);  // 直接退出进程！
    }
}
```

**设计理念**：在 Redis 社区的考虑中，`nodes.conf` 是集群元数据的**唯一存储选项**，需要尽量保障元数据的一致性。所以一旦这个文件更新失败，进程会"自杀"退出。

---

## 三、灾难场景：一次硬盘故障引发的集群雪崩

### 3.1 问题触发条件

硬盘本身是有一定故障率的。在云环境庞大的体量下，硬盘发生故障几乎是一件**必然发生的事情**。其中，**硬盘只读**是一类占比非常高的硬盘故障类型。

当硬盘变为只读后：

1. 集群中发生任何元数据变化
2. `nodes.conf` 的写盘/刷盘操作失败
3. 触发 `clusterSaveConfigOrDie` 的退出逻辑
4. 节点进程"自杀"

### 3.2 灾难场景分析

让我们从最简单的情况开始，逐步升级到更复杂的多节点场景。

#### 3.2.1 场景一：单分片的主节点自杀

即使是最简单的集群拓扑——一个分片只有一主一副本，硬盘故障也可能造成致命影响。

考虑以下时序：主节点所在的机器发生硬盘只读，恰好在这个时间窗口内，该主节点对应的副本节点因为其他原因（如 OOM、网络隔离等）发生故障，集群将副本标记为 `FAIL` 状态。这一状态变更需要更新 `nodes.conf`，而此时主节点的硬盘已经只读——保存失败，主节点自杀退出。

```
┌──────────────────────────────────────────────────────────────────┐
│                                                                  │
│   物理服务器 1 [硬盘只读!]            物理服务器 2                    │
│   ┌─────────────────────┐          ┌─────────────────────┐       │
│   │  Shard-1 (Primary)  │◄─────────│  Shard-1 (Replica)  │       │
│   │  :6379              │  复制     │  :6379              │       │
│   └──────────┬──────────┘          └──────────┬──────────┘       │
│              │                                │                  │
│              │  1. 副本节点故障                 │  2. 被标记 FAIL   │
│              │  3. 集群节点状态变更              │                  │
│              │  4. 保存 nodes.conf 失败         │                 │
│              │  5. 主节点自杀！                 │                  │
│              ▼                               ▼                  │
│        ┌───────────┐                   ┌───────────┐             │
│        │  自杀退出   │                   │   故障    │             │
│        └───────────┘                   └───────────┘             │
│                                                                  │
│   结果：主从全部不可用，集群 CLUSTERDOWN，无法自动恢复                  │
└──────────────────────────────────────────────────────────────────┘
```

**影响分析**：
- 副本故障在先，主节点因硬盘只读自杀在后
- 该分片**主从全部不可用**，没有可用副本进行自动故障转移
- 集群进入 `CLUSTERDOWN` 状态，**需要人工介入恢复**

> 这个场景看似需要"两个故障同时发生"才会触发，概率很低。但在云上庞大的实例规模下，**小概率事件会变成必然事件**。而且两个故障不需要严格同时——只要在硬盘只读期间发生任何集群元数据变更，主节点就会自杀。

**隐藏的运维困境**：

即使副本节点没有立刻故障，主节点所在机器的硬盘只读本身就已经让运维陷入被动。此时主节点处于"**定时炸弹**"状态——它还活着，但随时可能因为一次集群元数据变更而自杀。这意味着：

- 该分片的副本节点突然变成了**事实上的单点**：一旦副本在这个窗口期内发生任何故障，分片就会直接不可用
- 运维需要**争分夺秒**地处理，但又**不敢轻举妄动**：任何涉及集群元数据变更的操作（如手动故障转移、添加新副本）都可能触发主节点自杀
- 对于一主一副本的集群，**没有多余的副本作为安全冗余**，容错空间为零

更棘手的是，运维人员尝试补救的操作本身，也会成为压死骆驼的最后一根稻草：

**补救尝试一：对副本执行手动故障转移**

最直觉的应对方式是：既然主节点所在机器的硬盘有问题，那就把主节点切走——对健康的副本执行 `CLUSTER FAILOVER`。但手动故障转移的流程中，主节点需要对副本的提升请求进行**投票**，投票完成后会触发集群配置文件更新，此时主节点硬盘只读——保存失败，主节点自杀退出。

```
┌──────────────────────────────────────────────────────────────────┐
│                                                                  │
│   物理服务器 1 [硬盘只读!]            物理服务器 2                │
│   ┌─────────────────────┐          ┌─────────────────────┐       │
│   │  Shard-1 (Primary)  │◄─────────│  Shard-1 (Replica)  │       │
│   │  :6379              │  复制    │  :6379              │       │
│   └──────────┬──────────┘          └──────────┬──────────┘       │
│              │                                │                  │
│              │  2. 主节点为副本投票            │  1. CLUSTER      │
│              │  3. 触发 nodes.conf 更新        │     FAILOVER     │
│              │  4. 写入失败，主节点自杀！      │                  │
│              ▼                                ▼                  │
│        ┌───────────┐                   ┌───────────┐             │
│        │  自杀退出  │                   │ 提升为新  │             │
│        │           │                   │  主节点   │             │
│        └───────────┘                   └─────┬─────┘             │
│                                              │                   │
│                          5. 分片进入单主无副本状态                │
│                          6. 添加新副本，全量同步耗时长            │
│                          7. 同步期间新主节点"裸奔"                │
│                             → 如果再宕机，分片完全不可用！        │
│                                                                  │
│   结果：业务短暂报错 + 分片长时间处于无冗余保护状态              │
└──────────────────────────────────────────────────────────────────┘
```

虽然手动故障转移本身成功了，副本顺利提升为新主节点。但故障转移过程中主节点自杀退出，会导致业务请求**短暂报错**。更关键的是，故障转移后分片进入了**单主无副本**的危险状态。如果节点内存很大（如几十 GB），添加新副本时的全量同步可能需要**数分钟甚至更长**，在这段时间里新主节点没有任何冗余保护。

**补救尝试二：先给分片添加新副本**

另一种思路是：不做故障转移，先给分片添加一个新副本节点，增加冗余后再处理故障主节点。但往集群里添加新节点时（`CLUSTER MEET`），同样会引发集群元数据变更，触发所有节点更新 `nodes.conf`——主节点硬盘只读，写入失败，自杀退出。

```
┌──────────────────────────────────────────────────────────────────┐
│                                                                  │
│   物理服务器 1 [硬盘只读!]            物理服务器 2                │
│   ┌─────────────────────┐          ┌─────────────────────┐       │
│   │  Shard-1 (Primary)  │◄─────────│  Shard-1 (Replica)  │       │
│   │  :6379              │  复制    │  :6379              │       │
│   └──────────┬──────────┘          └──────────┬──────────┘       │
│              │                                │                  │
│              │  1. CLUSTER MEET 添加新节点     │                  │
│              │  2. 集群元数据变更              │                  │
│              │  3. 触发 nodes.conf 更新        │                  │
│              │  4. 写入失败，主节点自杀！      │                  │
│              ▼                                ▼                  │
│        ┌───────────┐                   ┌───────────┐             │
│        │  自杀退出  │                   │ 自动故障  │             │
│        │           │                   │ 转移提主  │             │
│        └───────────┘                   └───────────┘             │
│                                                                  │
│   相比手动故障转移，自动故障转移需经历判死 + 投票                 │
│      业务受损时间更长（数秒~数十秒）                              │
│                                                                  │
│   结果：业务中断时间更长 + 分片同样进入无冗余保护状态             │
└──────────────────────────────────────────────────────────────────┘
```

与手动故障转移场景相比，这里的区别在于主节点是**意外退出**，集群需要走完整的**自动故障转移流程**（节点判死 → 投票 → 副本提升），业务受损时间更长。同样，故障转移后分片也会进入单主无副本的危险状态。

> 这两个场景揭示了一个令人沮丧的现实：**运维人员无论怎么做都是错的**。不处理，主节点随时可能自杀；手动故障转移，投票过程触发自杀；添加新副本，元数据变更触发自杀。所有常规的补救手段都会成为引爆"定时炸弹"的导火索。

#### 3.2.2 场景二：单分片的副本节点自杀

与场景一相反，如果硬盘故障发生在**副本节点**所在的机器上，情况同样不容乐观。

当副本节点的硬盘变为只读后，一旦集群中发生任何元数据变更，副本节点就会因为 `nodes.conf` 写入失败而自杀退出。此时分片进入了**单主无副本**的状态。

```
┌──────────────────────────────────────────────────────────────────┐
│                                                                  │
│   物理服务器 1                        物理服务器 2 [硬盘只读!]    │
│   ┌─────────────────────┐          ┌─────────────────────┐       │
│   │  Shard-1 (Primary)  │─────────▶│  Shard-1 (Replica)  │       │
│   │  :6379              │  复制    │  :6379              │       │
│   └─────────────────────┘          └──────────┬──────────┘       │
│                                               │                  │
│              1. 集群中发生元数据变更            │                  │
│              2. 副本节点保存 nodes.conf 失败    │                  │
│              3. 副本节点自杀！                  │                  │
│                                               ▼                  │
│   ┌─────────────────────┐          ┌─────────────────────┐       │
│   │  Shard-1 (Primary)  │          │      自杀退出       │       │
│   │  :6379              │          └─────────────────────┘       │
│   │  单主无副本！       │                                        │
│   └──────────┬──────────┘                                        │
│              │                                                   │
│              │  4. 运维添加新副本，触发全量同步                   │
│              │  5. 大内存实例同步耗时长（数分钟甚至更久）         │
│              │  6. 同步期间主节点"裸奔"                           │
│              │     → 如果主节点此时宕机，分片完全不可用！         │
│                                                                  │
│   结果：分片失去冗余保护，长时间处于单主"裸奔"状态               │
└──────────────────────────────────────────────────────────────────┘
```

**影响分析**：
- 副本自杀退出后，分片**失去冗余保护**
- 添加新副本需要**全量同步**，对于大内存实例（如几十 GB 甚至更大），同步耗时可能长达**数分钟甚至十几分钟**
- 全量同步期间，新副本尚未就绪，无法参与故障转移
- 主节点在这段时间里**完全没有后备**，一旦宕机，分片直接不可用

> 虽然副本节点自杀不会立即导致业务中断（主节点仍在服务），但它悄然**剥夺了分片的故障转移能力**。这就像拆掉了安全网——走钢丝的人暂时没有掉下来，但一旦失足，就是万劫不复。对于大内存实例，这段没有安全网的"裸奔"时间会更加漫长。

#### 3.2.3 场景三：同一实例的多个节点部署在同一机器

云上的机器规格一般都很大，为了充分利用资源，一个机器上通常会部署**多个 Redis 节点**。

对于分片数较多的集群实例，可能会有同一个实例的多个节点分配到同一台机器上。在部署架构合理的情况下（同一分片的主从节点分布在不同机器），一台机器上可能部署了分片 A 的主节点和分片 B 的副本节点。

```
┌──────────────────────────────────────────────────────────────────┐
│                           物理服务器                              │
│   ┌──────────────────────────────────────────────────────────┐   │
│   │                   集群实例 A (N 分片)                     │   │
│   │     ┌──────────────┐           ┌──────────────┐          │   │
│   │     │  Shard-1     │           │  Shard-2     │          │   │
│   │     │  Master      │           │  Slave       │          │   │
│   │     │  :7001       │           │  :7002       │          │   │
│   │     └──────┬───────┘           └──────┬───────┘          │   │
│   │            │                          │                  │   │
│   └────────────┼──────────────────────────┼──────────────────┘   │
│                │                          │                      │
│                └────────────┬─────────────┘                      │
│                        ┌────▼────┐                               │
│                        │  硬盘   │ ← 只读故障！                  │
│                        │ (共享)  │                               │
│                        └─────────┘                               │
└──────────────────────────────────────────────────────────────────┘

注：Shard-1 的 Slave 和 Shard-2 的 Master 部署在其他机器上
```

**影响分析**：
- 该机器上有 1 个主节点（Shard-1）和 1 个从节点（Shard-2 的副本）
- 硬盘故障后，**2 个节点可能同时退出**
- 由于部署架构合理，Shard-1 的副本和 Shard-2 的主节点在其他机器上
- **故障转移后集群可恢复**：Shard-1 的副本提升为主节点，Shard-2 的主节点仍然可用
- **但在故障转移完成前**，业务会感知到部分流量访问报错

#### 3.2.4 场景四：多个实例的节点部署在同一机器

在云服务场景中，同一台物理机上往往会混合部署**多个不同实例**的节点，以提高资源利用率。以两个独立集群为例：

```
┌──────────────────────────────────────────────────────────────────┐
│                           物理服务器                              │
│                                                                  │
│   ┌──────────────────────────────────────────────────────────┐   │
│   │                   集群实例 A (业务 A)                     │   │
│   │     ┌──────────────┐           ┌──────────────┐          │   │
│   │     │  Shard-1     │           │  Shard-2     │          │   │
│   │     │  Master      │           │  Slave       │          │   │
│   │     │  :7001       │           │  :7002       │          │   │
│   │     └──────────────┘           └──────────────┘          │   │
│   └──────────────────────────────────────────────────────────┘   │
│                                                                  │
│   ┌──────────────────────────────────────────────────────────┐   │
│   │                   集群实例 B (业务 B)                     │   │
│   │     ┌──────────────┐           ┌──────────────┐          │   │
│   │     │  Shard-1     │           │  Shard-2     │          │   │
│   │     │  Master      │           │  Slave       │          │   │
│   │     │  :8001       │           │  :8002       │          │   │
│   │     └──────────────┘           └──────────────┘          │   │
│   └──────────────────────────────────────────────────────────┘   │
│                        ┌─────────────┐                           │
│                        │    硬盘     │ ← 只读故障！              │
│                        │   (共享)    │                           │
│                        └─────────────┘                           │
└──────────────────────────────────────────────────────────────────┘

注：每个集群实例的其他节点部署在其他机器上
```

**影响分析**：
- 该机器上有 **2 个不同集群实例**的 **4 个节点**
- 硬盘故障后，**4 个节点可能同时退出**
- **2 个不同的业务**可能同时受到影响
- 相比单实例场景，**影响范围扩大到多个业务**

#### 3.2.5 场景五：多台机器同时硬盘故障

在极端情况下，如果多台机器同时发生硬盘故障，可能导致同一集群的**主从节点同时退出**，造成集群完全不可用且无法自动恢复。

```
┌─────────────────────┐       ┌─────────────────────┐
│    物理服务器 1      │       │    物理服务器 2      │
│                     │       │                     │
│  Shard-1 (Master)   │       │  Shard-1 (Slave)    │
│  Shard-2 (Slave)    │       │  Shard-2 (Master)   │
│                     │       │                     │
│    [硬盘故障!]      │       │    [硬盘故障!]      │
└──────────┬──────────┘       └──────────┬──────────┘
           │                             │
           └──────────┬──────────────────┘
                      ▼
          ┌───────────────────────┐
          │ Shard-1: 主从全部退出 │
          │ Shard-2: 主从全部退出 │
          │                       │
          │ 集群 CLUSTERDOWN！    │
          │ 无法自动恢复！        │
          └───────────────────────┘
```

**可能的触发条件**：
- 同一批次硬盘出现批量故障
- 同一存储阵列或 SAN 故障
- 数据中心电力或散热故障

**影响分析**：
- 两台机器上共 **4 个节点同时退出**
- Shard-1 和 Shard-2 的**主从节点全部不可用**
- 集群进入 `CLUSTERDOWN` 状态，**无法自动故障转移**
- **需要人工介入**：修复硬盘或从备份恢复后才能恢复服务

**隐藏的运维困境**：

即使多台机器的硬盘故障没有严格同时发生，只要存在时间上的重叠，运维就会陷入**连锁困局**：

- **第一台机器故障后**，其上的节点进入"定时炸弹"状态。运维人员开始紧急处理，但此时**第二台机器也发生故障**——处理窗口被急剧压缩
- **故障互相触发**：第一台机器上的节点退出会引发集群元数据变更，这个变更广播到第二台故障机器上的节点，导致它们也因 `nodes.conf` 写入失败而自杀——形成**故障级联**
- **恢复难度倍增**：多个分片的主从全部不可用，不仅无法自动故障转移，人工恢复时还需要同时处理多台故障机器，**恢复时间远超单机故障**
- 在云上，同一批次采购的硬盘往往有**相近的寿命周期**，一块硬盘故障可能预示着同批次硬盘即将集中进入故障期

#### 3.2.6 影响范围与危害总结

**问题的核心在于**：一旦某台机器发生硬盘只读故障，只要集群中发生任何元数据变化（如节点故障、槽位迁移、故障转移等），新的元数据就会通过 Gossip 协议广播到集群中的所有节点。此时，这台故障机器上的多个节点会近乎同时尝试更新 `nodes.conf`，并因写入失败而"自杀"退出。这种连锁反应会同时影响多个分片的可用性，将原本的单点故障**放大为多点故障**。

更严重的是，如果多台机器同时发生硬盘故障（如同一批次硬盘、同一存储后端），这种连锁反应会**进一步放大**：一台机器上的节点退出触发集群元数据变更，导致其他故障机器上的节点也因无法更新 `nodes.conf` 而退出，最终可能导致某些分片的主从节点全部不可用，集群彻底瘫痪且无法自动恢复。

**各场景影响对比**：

| 场景 | 受影响节点数 | 受影响实例数 | 可否自动恢复 | 严重程度 |
|------|-------------|-------------|-------------|----------|
| 单分片主节点自杀 + 副本故障 | 2 | 1 | ❌ 无法自动恢复 | 🔴🔴 高 |
| 单分片副本自杀（分片失去冗余） | 1 | 1 | 暂时可用但无冗余 | 🔴 中 |
| 单实例多节点（单机故障） | 2+ | 1 | ✅ 可自动故障转移 | 🔴 中 |
| 多实例多节点（单机故障） | 4+ | 2+ | ✅ 可自动故障转移 | 🔴🔴 高 |
| 多机同时故障（主从同时退出） | 4+ | 1+ | ❌ 无法自动恢复 | 🔴🔴🔴 极高 |

> 注：假设部署架构合理，同一分片的主从节点分布在不同机器上。

**隐藏的硬盘依赖带来的危害**：

正是因为 `nodes.conf` 引入了对硬盘的强依赖，在运维层面需要比"纯内存数据库"考虑**更多的故障场景**：

| 原本的认知 | 实际需要考虑 |
|-----------|-------------|
| 只需关注内存和网络 | 还需关注硬盘健康状态 |
| 节点故障 = 进程崩溃或网络隔离 | 节点故障 = 进程崩溃 + 网络隔离 + **硬盘异常** |
| 主从分布在不同机器即可 | 还需考虑**硬盘故障域**（不同批次、不同存储后端） |
| 单机故障不影响集群可用性 | **多机关联故障**可能导致集群不可用 |

**在云上，这笔账更不划算**：

在云环境中，集群的元数据并不只有 `nodes.conf` 这唯一一份副本。云上的管控系统本身就维护着集群的完整拓扑信息——每个节点属于哪个分片、承载哪些 slot、主从关系如何，这些元数据都可以从管控侧恢复。换句话说，即使 `nodes.conf` 因硬盘故障没有写入成功，**集群元数据也不会真正丢失**。

这就使得问题的**性价比考量**发生了根本变化：

| 维度 | 社区视角（自建） | 云服务视角 |
|------|-----------------|-----------|
| 元数据恢复能力 | `nodes.conf` 是唯一存储，丢失代价极高 | 管控系统有完整元数据备份，可随时恢复 |
| 节点自杀的代价 | 保护元数据一致性，有一定合理性 | 造成业务中断、连锁故障，代价远大于收益 |
| 权衡结论 | 宁可退出也不能带着错误配置运行 | **`nodes.conf` 更新失败就自杀，不划算** |

在自建场景下，`nodes.conf` 是集群元数据的唯一可靠存储，节点选择"宁可自杀也不带着过时配置运行"有其合理性。但在云上，管控系统为元数据提供了**兜底保障**，这意味着 `nodes.conf` 写盘失败并不等于元数据不可恢复——而节点自杀带来的**业务中断、连锁故障、数据丢失**却是实实在在的损失。

> 这种"隐藏的硬盘依赖"打破了用户对内存数据库的直觉认知，增加了运维复杂度。这也是社区花费 15 个月讨论并最终引入 `cluster-persist-config` 配置项的重要原因。而在云上，管控系统的存在进一步改变了这个等式——保护元数据有了更优的方式，节点没有必要为此付出自杀的代价。

#### 3.2.7 灾难链条

无论是哪种部署场景，灾难的触发链条都是相似的：

```
某节点发生故障（或任何集群元数据变更）
    │
    ▼
集群状态变更消息通过 Gossip 协议广播到所有节点
    │
    ▼
所有节点尝试更新各自的 nodes.conf
    │
    ▼
故障服务器上的所有节点写入失败（共享同一块故障硬盘）
    │
    ▼
所有节点近乎同时触发 clusterSaveConfigOrDie
    │
    ▼
所有节点近乎同时"自杀"退出
    │
    ▼
┌───────────────────────────────────────────────────┐
│ • 单机故障：多个分片需要故障转移，可自动恢复       │
│ • 多机同时故障：主从同时退出，集群不可用，无法恢复 │
│ • 在故障转移完成前，业务感知到访问报错             │
└───────────────────────────────────────────────────┘
    │
    ▼
集群雪崩！多个业务同时受损！
```

#### 3.2.8 为什么会"同时"退出？

这里的"同时"并非绝对意义上的同一时刻，而是指**在极短的时间窗口内**（通常是毫秒到秒级）所有节点都会退出：

1. **Gossip 协议的广播特性**：集群状态变更会快速传播到所有节点
2. **nodes.conf 更新时机**：在 `clusterBeforeSleep` 中触发，每个事件循环都可能执行
3. **共享故障点**：同一硬盘的故障影响所有使用该硬盘的节点
4. **无退避机制**：原设计中没有重试或延迟机制，失败即退出

### 3.3 被动故障转移的困境

节点自杀后，Redis 自身有节点判死和故障转移逻辑来帮助集群恢复访问。但是：

| 步骤 | 耗时 | 说明 |
|------|------|------|
| 节点判死 | 数秒~数十秒 | 依赖节点的心跳超时发现异常 |
| 故障转移投票 | 数秒 | 需要集群中大多数 slot owner 投票达成一致 |
| 新主节点就位 | 数秒 | 从节点提升为主节点 |

**在故障转移完成前**，无论这个自杀的节点是主节点还是带有读流量的副本节点（针对开启读写分离的实例），业务都会感知到**全部或部分流量访问报错**，严重影响服务质量。

而且，故障转移只是恢复了分片的基本服务能力，**并不意味着恢复了高可用**。故障转移后，分片往往处于单主无副本的状态，需要添加新副本来重建冗余。对于**大内存实例**（如几十 GB 甚至上百 GB），新副本的全量同步可能耗时**数分钟甚至十几分钟**。在这段漫长的等待期间，分片没有任何冗余保护，如果主节点再次出现问题，就会彻底不可用。

可以看到，硬盘只读后触发的故障转移是在**没有任何准备**的情况下被动发生的。从节点自杀、到故障转移完成、再到新副本同步就绪，整个恢复链条可能长达**十几分钟甚至更久**，期间业务持续处于降级或无保护状态。

更值得警惕的是，被动故障转移不仅有**时间成本**，还有**数据成本**。Redis/Valkey 采用的是**异步复制**机制——主节点在执行完写命令并返回客户端成功后，才会异步地将数据传播给副本节点。当主节点因 `clusterSaveConfigOrDie` 突然自杀退出时，那些已经返回客户端"成功"、但尚未来得及复制到副本的写入数据，会随着主节点的退出而**永久丢失**。故障转移完成后，新提升的主节点上并不包含这部分数据，客户端却已经收到了写入成功的响应——这就造成了**数据不一致**。对于涉及资金、库存、计数器等对数据准确性要求极高的业务场景，这种"静默丢失"的后果可能远比短暂的服务中断更加严重。

站在 Redis/Valkey 社区的角度，`nodes.conf` 是集群元数据的唯一存储选项，优先保护集群元数据的一致性无可厚非。但站在云服务的角度，我们不仅仅是为客户提供社区数据库的托管服务，更需要让客户享受到比自建更高的**便利性**和**稳定性**。

因此，我们需要设计一套**主动且优雅**的方案：
- 在硬盘故障发生时，**阻止节点被动自杀**
- **主动**将受影响的节点迁移到健康的机器上
- 让客户受到的影响**降到最小**，甚至**无感知**

> 这也是后文将要介绍的腾讯云 CRedis 方案的核心思想：**化被动为主动**。

### 3.4 用户认知断层

正如 enjoy-binbin 在 PR 讨论中指出的：

> **"普通用户认为 Redis/Valkey 是内存数据库，在禁用持久化（RDB 和 AOF）后，很难接受因为配置文件保存失败导致的集群故障。"**

这种**认知断层**是问题的根源之一：用户期望内存数据库不依赖硬盘，但实际上 `nodes.conf` 的保存逻辑打破了这个假设。

---

## 四、社区的解决方案

针对这个问题，Valkey 社区计划在 9.1 版本中引入 `cluster-persist-config` 配置项，让集群管理员可以**控制 `nodes.conf` 保存失败时的行为**。

通过这个配置项，管理员可以选择：
- **保持原有行为**（默认）：保存失败时进程退出，严格保证元数据一致性
- **优先可用性**：保存失败时仅记录日志并继续运行，避免节点"自杀"

此外，社区讨论中还提出了更彻底的方案：**无盘集群模式**，即完全不读写 `nodes.conf`。这种模式适用于使用外部控制平面（如 Kubernetes Operator）维护集群的场景，从根本上消除对硬盘的依赖。

然而，社区方案存在一个明显的局限：**只能影响新版本**。对于云服务商而言，线上运行着大量的老版本实例，无法立即升级。为此，腾讯云 CRedis 团队设计了一套**不依赖版本升级**的运维方案。

---

## 五、解决方案：从社区修复到工业级实践

面对硬盘故障导致的集群可用性问题，存在两种截然不同的解决思路：

1. **社区修复方案**：通过修改源码，从根本上改变 `nodes.conf` 写入失败时的行为，适用于新版本
2. **云上运维方案**：在不修改源码的情况下，通过运行时 Hook 技术规避问题，适用于存量老版本实例

```
┌──────────────────────────────────────────────────────────────────┐
│                       解决方案对比总览                            │
├──────────────────────────────────────────────────────────────────┤
│                                                                  │
│   ┌─────────────────────┐         ┌─────────────────────┐       │
│   │   社区修复方案       │         │  云上运维方案        │       │
│   │   (Valkey 9.1+)     │         │  (腾讯云 CRedis)    │       │
│   ├─────────────────────┤         ├─────────────────────┤       │
│   │ • 修改源码          │         │ • Module 动态 Hook  │       │
│   │ • 新版本生效        │         │ • 所有版本适用      │       │
│   │ • 一次修复永久生效  │         │ • 无需升级实例      │       │
│   │ • 需要升级版本      │         │ • 配合无缝迁移      │       │
│   └──────────┬──────────┘         └──────────┬──────────┘       │
│              │     ┌───────────────────┐     │                  │
│              └────▶│     共同目标      │◄────┘                  │
│                    │  硬盘故障时       │                         │
│                    │  节点不退出       │                         │
│                    │  保持集群可用     │                         │
│                    └───────────────────┘                         │
│                                                                  │
└──────────────────────────────────────────────────────────────────┘
```

### 5.1 社区解决方案：Valkey 9.1 的 cluster-persist-config 配置项

对于新版本用户来说，升级到 Valkey 9.1 是最简单直接的解决方案。社区通过引入 `cluster-persist-config` 配置项，让用户可以在一致性和可用性之间做出选择。

#### 5.1.1 配置项详解

**配置语法**：

```
cluster-persist-config <sync|best-effort>
```

**配置选项**：

| 选项 | 默认 | 行为 | 适用场景 |
|------|------|------|----------|
| **`sync`** | ✅ 是 | 同步保存配置，**失败则退出** | 严格一致性要求、向后兼容 |
| **`best-effort`** | | 同步保存配置，**失败仅记录日志** | 高可用性要求、云环境 |

**配置示例**：

```bash
# 在 valkey.conf 中配置
cluster-persist-config best-effort

# 或通过命令行参数
valkey-server --cluster-persist-config best-effort

# 运行时动态修改（支持热生效）
127.0.0.1:6379> CONFIG SET cluster-persist-config best-effort
OK
127.0.0.1:6379> CONFIG GET cluster-persist-config
1) "cluster-persist-config"
2) "best-effort"
```

#### 5.1.2 核心代码实现

**新增枚举类型**（`src/server.h`）：

```c
/* Cluster persist config mode */
typedef enum {
    CLUSTER_PERSIST_CONFIG_SYNC = 0,       /* 同步保存，失败退出（默认，老行为） */
    CLUSTER_PERSIST_CONFIG_BEST_EFFORT     /* 同步保存，失败仅记录日志 */
} cluster_persist_config_mode;
```

**新增保存函数**（`src/cluster_legacy.c`）：

```c
/* 原有函数：保存失败则退出 */
void clusterSaveConfigOrDie(int do_fsync) {
    if (clusterSaveConfig(do_fsync) == C_ERR) {
        serverLog(LL_WARNING, "Fatal: can't update cluster config file.");
        exit(1);  // 直接退出进程
    }
}

/* 新增函数：保存失败仅记录日志 */
#define CONFIG_SAVE_LOG_ERROR_RATE 30 /* 日志限流：每 30 秒最多记录一次 */

void clusterSaveConfigOrLog(int do_fsync) {
    if (clusterSaveConfig(do_fsync) == C_ERR) {
        static time_t last_save_error_log = 0;
        /* 限制日志频率，防止日志刷屏 */
        if ((server.unixtime - last_save_error_log) > CONFIG_SAVE_LOG_ERROR_RATE) {
            serverLog(LL_WARNING, 
                "Cluster config updated even though writing "
                "the cluster config file to disk failed.");
            last_save_error_log = server.unixtime;
        }
    }
}
```

**根据配置选择保存函数**（`src/cluster_legacy.c` - `clusterBeforeSleep`）：

```c
if (flags & CLUSTER_TODO_SAVE_CONFIG) {
    int fsync = flags & CLUSTER_TODO_FSYNC_CONFIG;
    if (server.cluster_persist_config == CLUSTER_PERSIST_CONFIG_SYNC) {
        /* Sync 模式：保存失败则退出 */
        clusterSaveConfigOrDie(fsync);
    } else if (server.cluster_persist_config == CLUSTER_PERSIST_CONFIG_BEST_EFFORT) {
        /* Best-effort 模式：保存失败仅记录日志，等待下次重试 */
        clusterSaveConfigOrLog(fsync);
    }
}
```

#### 5.1.3 特殊处理：启动阶段仍强制退出

即使配置了 `best-effort` 模式，在以下启动阶段的保存操作中，仍强制使用 `clusterSaveConfigOrDie`：

| 调用位置 | 说明 | 原因 |
|----------|------|------|
| `clusterInit()` | 集群初始化 | 启动时硬盘异常是致命问题 |
| `verifyClusterConfigWithData()` | 验证集群配置数据 | 配置验证失败不应继续 |

```c
// clusterInit 中的调用
if (saveconf) clusterSaveConfigOrDie(1);  // 始终使用 OrDie 版本
```

**设计理念**：启动时的硬盘问题表明环境存在严重问题，应该立即失败（Fail-Fast），而不是带病运行。运行时的硬盘问题则可以容忍，因为节点已经在正常工作，应该尽量保持服务可用。

#### 5.1.4 两种模式的行为对比

| 场景 | `sync` 模式（默认） | `best-effort` 模式 |
|------|---------------------|-------------------|
| **启动时保存失败** | 进程退出 | 进程退出（强制） |
| **运行时保存失败** | 进程退出 | 记录警告日志，继续运行 |
| **日志输出** | `Fatal: can't update cluster config file.` | `Cluster config updated even though writing the cluster config file to disk failed.`（限流：每 30 秒一次） |
| **集群可用性** | 可能雪崩 | 保持可用 |
| **管理员响应时间** | 无（已宕机） | 有充足时间故障排查和迁移 |
| **元数据一致性** | 严格保证 | 可能短暂不一致 |

#### 5.1.5 安全机制：safe_to_join

为了防止过时配置导致的问题，社区还讨论了引入 `safe_to_join` 机制：

```c
// 在 clusterState 结构体中新增字段
struct clusterState {
    // ... 其他字段
    int safe_to_join; /* 重启的节点是否可以安全加入集群？ */
};
```

这个机制确保：
- 重启后的节点**不会立即参与故障转移投票**
- 需要等待一定时间确认集群状态后才能"安全加入"
- 在 `clusterSendFailoverAuthIfNeeded` 中增加检查：如果 `!server.cluster->safe_to_join`，拒绝投票

#### 5.1.6 nodes.conf 没有及时更新，真的很重要吗？

读到这里，你可能会有一个疑问：既然 `best-effort` 模式下 `nodes.conf` 可能不一致，而腾讯云 CRedis 方案更是直接 Mock 掉了写入函数——**过时的 `nodes.conf` 会不会导致严重问题？**

答案是：**短期内不会。** 集群的 epoch 机制天然就是为了处理这种不一致而设计的。

##### nodes.conf 过时的本质

`nodes.conf` 过时，本质上等价于一个更常见的场景：**节点在集群经历了多次变更后重启**。

想象这样一个场景：某个节点因为某些原因下线了一段时间（比如硬件维修、网络隔离），在它下线的这段时间里，集群发生了多次故障转移、槽位迁移等变更。当这个节点重新启动时，它从本地 `nodes.conf` 加载到的集群视图，自然是**过时的**——它不知道分片 A 已经发生了故障转移，分片 B 的主节点已经换了一个人，集群的 currentEpoch 也早已递增了好多轮。

这和硬盘只读期间 `nodes.conf` 无法更新的效果是完全一样的：节点手里拿着一份"旧地图"。

##### epoch 机制如何修正过时配置

Redis/Valkey 集群的 Gossip 协议和 epoch 机制，天然就能处理这种"旧地图"问题。节点重启后，集群会通过以下机制快速修正它的过时视图：

**1. currentEpoch 追赶**

节点收到任何集群消息时，会检查发送方的 `currentEpoch`。如果对方的更大，就直接更新自己的：

```c
// clusterProcessPacket 中
if (sender_claimed_current_epoch > server.cluster->currentEpoch)
    server.cluster->currentEpoch = sender_claimed_current_epoch;
```

这意味着过时节点只需要收到**一条**来自集群的消息，就能立即跟上最新的全局 epoch。

**2. configEpoch 和槽位所有权更新**

当过时节点声称自己拥有某些 slot 时，其他节点会比对 configEpoch。如果有另一个节点以**更高的** configEpoch 声明拥有同一批 slot，集群会自动将 slot 所有权更正为新的主人：

```c
// clusterUpdateSlotsConfigWith 中
// 如果 slot 无主，或者新声明者的 configEpoch 更高，就重新绑定 slot
if (isSlotUnclaimed(j) ||
    server.cluster->slots[j]->configEpoch < senderConfigEpoch) {
    // 将 slot 重新绑定到拥有更高 configEpoch 的节点
}
```

**3. UPDATE 消息的主动纠正**

更巧妙的是，如果集群中的其他节点发现这个重启的节点拿着过时的 slot 声明（即它声明的 slot 已经被 configEpoch 更高的节点接管），会**主动**给它发送 `UPDATE` 消息来纠正：

```c
// clusterProcessPacket 中处理 PING/PONG 响应时
if (slot_owner->configEpoch > sender_claimed_config_epoch) {
    // 发现 sender 拿着过时的 slot 配置
    // 主动发送 UPDATE 消息告知正确的 slot 归属
    clusterSendUpdate(sender->link, slot_owner);
}
```

这就像一个刚从休假回来的同事，带着一份旧版组织架构图来上班。他不需要自己去查所有的变动——其他同事在日常沟通中（Gossip）就会自然而然地告诉他："嘿，这个项目已经换负责人了"。

##### 安全保护：重启后的冷静期

为了避免拿着"旧地图"的节点做出错误决策，Valkey 还引入了两道安全屏障：

**屏障一：`CLUSTER_WRITABLE_DELAY`（2 秒冷静期）**

主节点重启后，即使加载了 `nodes.conf`，也不会立即将集群状态标记为 `OK`。它会等待 **2 秒钟**的冷静期，在此期间 `safe_to_join` 标志为 `false`：

```c
#define CLUSTER_WRITABLE_DELAY 2000 // 2 秒

if (clusterNodeIsPrimary(myself) &&
    server.cluster->state == CLUSTER_FAIL &&
    mstime() - first_call_time < CLUSTER_WRITABLE_DELAY) {
    server.cluster->safe_to_join = 0;  // 暂不安全
    return;
}
```

**屏障二：`safe_to_join` 投票保护**

在冷静期内，节点**拒绝为任何故障转移投票**，防止基于过时信息做出错误的故障转移决策：

```c
// clusterSendFailoverAuthIfNeeded 中
if (!server.cluster->safe_to_join) {
    serverLog(LL_WARNING,
        "Failover auth denied to %.40s: "
        "it is not safe to vote in this moment");
    return;  // 拒绝投票
}
```

**屏障三：主节点的重新加入延迟**

如果主节点曾处于少数派分区（minority partition），在分区恢复后，它不会立即将集群状态切换为 `OK`，而是额外等待一个 `rejoin_delay`（最长 5 秒），给自己留出足够的时间来接收最新的集群配置：

```c
if (new_state == CLUSTER_OK &&
    clusterNodeIsPrimary(myself) &&
    mstime() - among_minority_time < rejoin_delay) {
    return;  // 延迟重新加入，先更新配置
}
```

##### 用一张图理解整个过程

```
┌──────────────────────────────────────────────────────────────────┐
│          nodes.conf 过时节点的自愈过程（以重启为例）               │
├──────────────────────────────────────────────────────────────────┤
│                                                                  │
│   节点重启，加载过时的 nodes.conf                                │
│   (currentEpoch=5, 认为自己负责 slot 0-5460)                    │
│        │                                                        │
│        ▼                                                        │
│   ┌─────────────────────────────────────────────┐               │
│   │  冷静期 (2秒)                                │               │
│   │  • safe_to_join = false                     │               │
│   │  • 拒绝投票，不接受写入                      │               │
│   │  • 但可以接收 Gossip 消息                    │               │
│   └──────────────────────┬──────────────────────┘               │
│                          │                                      │
│                          ▼                                      │
│   收到第一条集群消息 (来自任意节点)                              │
│        │                                                        │
│        ├── currentEpoch 追赶: 5 → 42                            │
│        ├── 发现 slot 0-5460 已被 configEpoch=38 的新主节点接管   │
│        └── 收到 UPDATE 消息，更正本地 slot 归属表                │
│                          │                                      │
│                          ▼                                      │
│   几轮 Gossip 交换后，节点视图与集群完全一致                    │
│   nodes.conf 更新为最新状态                                     │
│                                                                  │
│   整个过程：通常在数秒内完成，不需要人工干预                    │
│                                                                  │
└──────────────────────────────────────────────────────────────────┘
```

##### 所以，为什么说"不急"？

综合以上分析，`nodes.conf` 短期不一致在集群设计层面是**被允许的**：

| 维度 | 说明 |
|------|------|
| **epoch 机制** | 集群天然支持从过时状态收敛到最新状态，高 epoch 胜出 |
| **UPDATE 消息** | 其他节点会主动纠正持有过时信息的节点 |
| **冷静期保护** | 重启节点不会立即做出可能错误的决策（投票、接受写入） |
| **最终一致性** | 通过持续的 Gossip 交换，所有节点最终会收敛到一致状态 |

`nodes.conf` 更像是一个**快照缓存**：它加速了节点重启后的恢复过程（不需要从零开始发现集群），但即使这个快照过时了，节点也能通过集群的 epoch 机制在**数秒内**自动修正到最新状态。

当然，这并不意味着 `nodes.conf` 完全不重要——如果它长期不更新且节点频繁重启，每次都需要重新走一遍修正流程，增加了不必要的开销和短暂的不确定窗口。所以在 CRedis 方案中，Mock 掉自杀函数后需要**尽快完成节点迁移**，而不是让节点长期运行在 `nodes.conf` 无法更新的状态下。

#### 5.1.7 使用建议

| 场景 | 推荐配置 | 原因 |
|------|----------|------|
| **生产环境（云服务）** | `best-effort` | 优先保证服务可用性 |
| **生产环境（自建）** | `best-effort` | 给管理员留出处理时间 |
| **测试/开发环境** | `sync` | 尽早发现硬盘问题 |
| **对一致性要求极高** | `sync` | 严格保证元数据一致 |
| **Kubernetes 环境** | `best-effort` | 配合外部控制平面 |

#### 5.1.8 方案优缺点

**优点**：
- ✅ **官方支持**：经过社区充分讨论和 TSC 团队批准
- ✅ **灵活可配**：用户可根据需求选择合适的模式
- ✅ **向后兼容**：默认 `sync` 模式保持与老版本行为一致
- ✅ **热生效**：支持运行时通过 `CONFIG SET` 动态修改
- ✅ **安全机制**：启动阶段仍强制退出，避免带病运行

**局限性**：
- ❌ **版本要求**：仅适用于 Valkey 9.1 及以上版本
- ❌ **升级成本**：存量老版本实例需要升级才能享受此功能
- ❌ **风险提示**：`best-effort` 模式下，`nodes.conf` 可能短暂不一致

---

### 5.2 云上运维方案：腾讯云 CRedis 的实践

虽然 Valkey 社区在 9.1 版本中修复了这个问题，但在云上运维场景里，存在大量的**老版本存量实例**。这些实例整体升级的代价太大，但如果不升级就无法享受到新的代码变更。

腾讯云 CRedis 团队设计了一套**兼顾不同历史版本的通用方案**，核心思想是：**发现异常后立即介入，化被动为主动**。

```
┌──────────────────────────────────────────────────────────────────┐
│                     从被动到主动的转变                            │
├──────────────────────────────────────────────────────────────────┤
│                                                                  │
│   【被动模式 - 原生行为】          【主动模式 - CRedis 方案】    │
│                                                                  │
│   硬盘只读发生                     硬盘只读发生                  │
│        │                                │                        │
│        ▼                          ┌─────────────┐                │
│   集群元数据变更                  │ 健康检测系统│                │
│        │                          │  主动发现   │                │
│        ▼                          └──────┬──────┘                │
│   nodes.conf 写入失败                    │                       │
│        │                          ┌──────▼──────┐                │
│        ▼                          │ 动态加载    │                │
│   节点自杀退出 ← 不可控！        │ Module Hook │                │
│        │                          │  规避自杀   │                │
│        ▼                          └──────┬──────┘                │
│   被动故障转移                           │                       │
│        │                          ┌──────▼──────┐                │
│        ▼                          │  无缝迁移   │                │
│   业务中断                        │  节点替换   │                │
│                                   └──────┬──────┘                │
│                                          │                       │
│                                          ▼                       │
│                                     业务无感知                   │
│                                                                  │
└──────────────────────────────────────────────────────────────────┘
```

#### 5.2.1 核心挑战

要在不修改源码的情况下解决这个问题，面临以下挑战：

| 挑战 | 说明 | 尝试过的方案 |
|------|------|--------------|
| **存量实例无法全部升级** | 云上已经跑了非常多的实例，整体升级代价巨大 | - |
| **配置项无法动态修改** | `cluster-config-file` 是不可修改的配置项，无法通过 `CONFIG SET` 在运行时修改 | 尝试使用 Module 非标修改，但无法覆盖所有场景 |
| **云盘方案不可靠** | 系统盘故障时，云盘不能百分百保证能挂载 | 将 `cluster-config-file` 指向云盘 |
| **tmpfs 方案有风险** | RDB 文件落盘可能会打爆内存 | 将目录指向内存文件系统 |

#### 5.2.2 解决方案：两步走策略

腾讯云 CRedis 采用"**规避 → 迁移**"两步走策略：当监控系统发现硬盘异常后，**立即**动态加载 Module 进行 Hook 规避自杀，然后启动无缝迁移流程将受影响节点替换到健康机器上。

```
┌──────────────────────────────────────────────────────────────────┐
│                  CRedis 硬盘只读处理流程                         │
├──────────────────────────────────────────────────────────────────┤
│                                                                  │
│                  ┌────────────────────┐                          │
│                  │  监控发现硬盘异常  │                          │
│                  └─────────┬──────────┘                          │
│                            │                                     │
│  ┏━━━━━━━━━━━━━━━━━━━━━━━━▼━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┓   │
│  ┃  第一步：规避自杀（动态 Hook）                             ┃   │
│  ┗━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┛   │
│                            │                                     │
│                    ┌───────▼────────┐                            │
│                    │  动态加载辅助  │                            │
│                    │   Module       │                            │
│                    │ (位于 tmpfs)   │                            │
│                    └───────┬────────┘                            │
│                            │                                     │
│                    ┌───────▼────────┐                            │
│                    │   动态 Hook    │                            │
│                    │ clusterSave   │                            │
│                    │ ConfigOrDie   │                            │
│                    └───────┬────────┘                            │
│                            │                                     │
│               硬盘故障场景下，节点不会自杀                       │
│                            │                                     │
│  ┏━━━━━━━━━━━━━━━━━━━━━━━━▼━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┓   │
│  ┃  第二步：无缝迁移（主动替换）                              ┃   │
│  ┗━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┛   │
│                            │                                     │
│                    ┌───────▼────────┐                            │
│                    │ 启动无缝迁移   │                            │
│                    │ (分角色处理)   │                            │
│                    └────────────────┘                            │
│                                                                  │
└──────────────────────────────────────────────────────────────────┘
```

##### 第一步：规避自杀 - 动态加载 Module

当监控系统发现硬盘异常后，**立即**向受影响节点动态加载一个辅助 Module，通过 Hook 技术 mock 掉 `clusterSaveConfigOrDie` 函数。这样，在后续迁移过程中，即使发生集群元数据变更，节点也不会因为 `nodes.conf` 写入失败而"自杀"。

```c
// 使用空函数替换原有的自杀函数
void mock_clusterSaveConfigOrDie(int do_fsync) {
    // 空实现，不做任何事情
    // 这样当需要更新 nodes.conf 时，不会走到进程退出的逻辑
}
```

**关键技术细节**：

| 要点 | 说明 |
|------|------|
| **Module 存储位置** | 位于 **tmpfs**（内存文件系统），确保硬盘只读后 Module 仍可正常加载 |
| **动态加载策略** | 监控发现硬盘异常后立即加载，无需重启节点，运行时即时生效 |
| **短期安全性** | 短时间内不依赖 `nodes.conf`，无缝迁移过程中是安全的 |
| **长期风险** | `nodes.conf` 不能长期不同步，需要尽快完成迁移 |

> **时间窗口的风险**
>
> 从硬盘故障发生到监控系统发现并加载 Module，存在一个**时间窗口**。在这个窗口期内，如果恰好发生集群元数据变更（如其他节点故障、槽位迁移等），受影响的节点仍然可能触发 `clusterSaveConfigOrDie` 而退出。不过在实际生产环境中，监控系统的探测间隔通常在**秒级**，而集群元数据变更并不是每时每刻都在发生，因此这个时间窗口的实际风险是**可控的**。

> **为什么不能长期不同步？**
> 
> `nodes.conf` 仍然很重要：
> - 监控组件会读取本地 `nodes.conf` 来感知节点信息，减少 `CLUSTER NODES` 命令的 QPS
> - 如果要原地重启进程，`nodes.conf` 能加快节点恢复速度
> 
> 所以在 Mock 掉自杀函数后，需要尽快完成节点迁移。

##### 第二步：无缝迁移 - 主动替换

由于第一步已经规避了节点自杀，此时可以**从容地**进行迁移操作。

管控会根据节点的权重和优先级，有限制地并行进行无缝迁移。为了在迁移过程中不影响客户业务，管控还会加载一些辅助 Module（无缝迁移模块、readonly 模块等），确保整个过程对业务**透明无感知**。

**普通副本节点**（实例没开副本只读）：

```
┌─────────────────────────────────────────────────────┐
│  1. 给这个分片增加一个新的副本节点                   │
│                    │                                │
│                    ▼                                │
│  2. 等待新的副本节点就绪（数据同步完成）             │
│                    │                                │
│                    ▼                                │
│  3. 下线受影响的老副本节点                           │
└─────────────────────────────────────────────────────┘
```

**只读副本节点**（实例开启副本只读）：

```
┌─────────────────────────────────────────────────────┐
│  1. 通知 proxy 这个副本节点将要下线                  │
│     → 逐步停掉它的读流量（流量摘除）                 │
│                    │                                │
│                    ▼                                │
│  2. 给这个分片增加一个新的副本节点                   │
│                    │                                │
│                    ▼                                │
│  3. 等待新的副本节点就绪                             │
│                    │                                │
│                    ▼                                │
│  4. 下线受影响的老副本节点                           │
└─────────────────────────────────────────────────────┘
```

**主节点**：

```
┌─────────────────────────────────────────────────────┐
│  1. 从所有副本节点中挑选一个合适的                   │
│                    │                                │
│                    ▼                                │
│  2. 发送 CLUSTER FAILOVER 让这个副本提主             │
│     → 老主节点降为副本节点                           │
│                    │                                │
│                    ▼                                │
│  3. 走副本节点的迁移流程                             │
└─────────────────────────────────────────────────────┘
```

#### 5.2.3 方案优缺点

**优点**：
- ✅ **版本无关**：适用于所有 Redis/Valkey 版本，包括老版本存量实例
- ✅ **热加载**：无需重启实例，运行时动态生效
- ✅ **无缝迁移**：不仅规避问题，还能主动完成节点替换
- ✅ **业务无感知**：配合流量摘除，迁移过程对业务透明

**局限性**：
- ❌ 需要管控系统支持迁移流程
- ❌ 方案复杂度较高
- ❌ 存在从故障发生到 Module 加载的短暂时间窗口

---

### 5.3 两种方案的对比与选择

| 维度 | 社区修复方案（Valkey 9.1+） | 云上运维方案（腾讯云 CRedis） |
|------|---------------------------|------------------------------|
| **适用版本** | ≥ 9.1 | 所有版本 |
| **实现方式** | 源码修改 | Module 动态 Hook |
| **部署成本** | 需要升级版本 | 无需升级，热加载 |
| **响应方式** | 一次性修复，无需额外响应 | 发现硬盘异常后动态加载 Module |
| **nodes.conf 处理** | 继续尝试写入（不退出） | Mock 掉写入函数 |
| **后续处理** | 等待管理员处理 | 自动触发无缝迁移流程 |
| **业务影响** | 节点继续运行 | 节点继续运行 + 无缝迁移 |
| **适用场景** | 新部署或可升级的环境 | 云上大量存量实例 |

**选择建议**：

- **新部署**：直接使用 Valkey 9.1+ 版本，享受社区修复
- **可升级环境**：优先考虑升级到 Valkey 9.1+
- **存量老版本**：参考腾讯云 CRedis 方案，实现 Module Hook + 无缝迁移
- **混合环境**：两种方案可以并存，新版本靠社区修复，老版本靠运维方案兜底

---

## 六、设计哲学：一致性与可用性的权衡

### 6.1 CAP 定理的实践

这个问题本质上是 **CAP 定理**在实践中的体现：

**原有设计（倾向一致性）**：
- 优先保证配置一致性：宁可退出，也不允许配置不一致
- 悲观策略：假设配置不一致会导致严重问题

**新设计（倾向可用性）**：
- 优先保证服务可用：即使配置可能过时，也要保持服务
- 乐观策略：相信 epoch 机制能够处理大部分不一致情况

### 6.2 被动故障 vs 主动容错

| 维度 | 原有设计（被动） | 新设计（主动） |
|------|-----------------|----------------|
| **故障响应** | 节点自杀，等待故障转移 | 继续运行，争取时间 |
| **管理员角色** | 事后救火 | 有时间主动处理 |
| **业务影响** | 不可预期的中断 | 可控的迁移窗口 |
| **设计理念** | Fail-Fast | Graceful Degradation |

### 6.3 配置与状态分离的思考

PingXie 在讨论中指出一个重要问题：

> "当前设计将配置（如 IP/端口、槽位）与状态（如 FAIL/PFAIL）混合在同一个文件中是有问题的。"

这引发了对未来架构改进的思考：
- 是否应该将 `nodes.conf` 拆分为配置文件和状态文件？
- 状态信息是否需要持久化？
- 是否应该支持完全的"无盘集群模式"？

---

## 七、生产环境最佳实践

### 7.1 监控硬盘健康状态

```bash
# 监控硬盘空间
df -h /var/lib/redis/

# 监控硬盘 IO 错误
dmesg | grep -i "error\|fail" | grep -i "sd\|nvme"

# 监控文件系统状态（检查是否有只读挂载）
mount | grep "ro,"

# 使用 smartctl 检查硬盘健康
smartctl -H /dev/sda
```

### 7.2 监控 Redis/Valkey 日志

配置告警规则，监控关键日志：

```bash
# Valkey 9.1+ 的警告日志
grep "Cluster config updated even though writing.*failed" /var/log/redis/redis.log

# 传统版本的致命日志（如果出现说明节点即将退出）
grep "Fatal: can't update cluster config file" /var/log/redis/redis.log
```

### 7.3 部署架构建议

```yaml
# 推荐的部署架构
deployment:
  # 1. 主从节点分布在不同物理机
  anti_affinity:
    - master 和对应的 slave 不在同一台机器
    - 同一分片的多个 slave 分散部署
  
  # 2. 使用独立的存储
  storage:
    - 每个 Redis 实例使用独立的硬盘/SSD
    - 或者考虑将 nodes.conf 放在高可靠存储上
  
  # 3. 考虑使用 tmpfs（如果可以接受重启后重新加入集群）
  tmpfs_option:
    - 将 cluster-config-file 指向 tmpfs
    - 需要配合外部控制平面管理集群配置
```

### 7.4 Kubernetes 环境特别注意

```yaml
# 方案一：使用 emptyDir（重启后丢失）
volumes:
  - name: cluster-config
    emptyDir: {}

# 方案二：使用 PVC（需要注意存储后端的可靠性）
volumes:
  - name: cluster-config
    persistentVolumeClaim:
      claimName: redis-config-pvc

# 方案三：使用 emptyDir 的 memory 后端
volumes:
  - name: cluster-config
    emptyDir:
      medium: Memory
      sizeLimit: 100Mi
```

### 7.5 应急预案

**场景**：发现硬盘只读，且 Redis 版本 < Valkey 9.1

**应急步骤**：

1. **立即评估影响范围**：
   ```bash
   # 检查该机器上有多少 Redis 实例
   ps aux | grep redis-server | grep -v grep
   ```

2. **冻结集群变更**：
   - 暂停任何计划中的槽位迁移
   - 暂停任何节点扩缩容操作

3. **准备迁移**：
   - 为受影响的主节点准备手动故障转移
   - 为从节点准备新的副本

4. **执行迁移**：
   ```bash
   # 主节点：触发手动故障转移
   redis-cli -h <slave-ip> -p <slave-port> CLUSTER FAILOVER
   
   # 从节点：添加新副本后删除老副本
   redis-cli -h <new-slave> CLUSTER REPLICATE <master-id>
   redis-cli CLUSTER FORGET <old-slave-id>
   ```

5. **验证集群状态**：
   ```bash
   redis-cli CLUSTER INFO
   redis-cli CLUSTER NODES
   ```

---

## 八、总结与展望

### 8.1 关键发现

1. **Redis/Valkey 集群并非完全的内存数据库**
   - 即使禁用所有持久化，`nodes.conf` 的写入仍然是必需的
   - 这是一个广泛存在的用户认知盲区

2. **硬盘故障可能导致集群雪崩**
   - 单个硬盘故障可能导致同一服务器上的所有实例同时退出
   - 被动的故障转移无法及时恢复服务

3. **设计权衡是复杂的**
   - 这个看似简单的问题背后，涉及一致性 vs 可用性的架构决策
   - 社区花费 15 个月讨论，充分说明了问题的复杂性

4. **工业级方案需要更全面**
   - 社区修复解决了"不崩溃"的问题
   - 云服务商还需要考虑无缝迁移、版本兼容等实际问题

### 8.2 为什么有了故障转移还要做这个优化？

可能有人会问：Redis Cluster 本身就有自动故障转移机制，节点挂了副本会自动提升为主节点，为什么还要花这么大力气去做这个优化？

自动故障转移并不是瞬间完成的——从节点判死、到投票选举、再到新主节点就位，整个过程需要**数秒到数十秒**。**在云上，每一秒都很重要。**

#### 8.2.1 硬盘故障是必然事件

单块硬盘的故障概率确实很小。但在云上，机器数量庞大，当基数足够大时，**小概率事件就变成了必然事件**。硬盘故障不是"会不会发生"的问题，而是"**什么时候发生**"的问题。

#### 8.2.2 极端场景下，集群故障无法自动恢复

正如前面的灾难场景分析所示，硬盘故障并不总是"挂一个节点、自动切一下"那么简单。当必然事件叠加——多台机器同时硬盘故障、或者硬盘故障恰好撞上副本异常——集群可能陷入**主从全部不可用**的境地，此时自动故障转移机制完全失效，集群进入 `CLUSTERDOWN` 状态，**只能依赖人工介入恢复**。

我们需要保证的不仅仅是"大多数情况下能自动恢复"，而是**在任何场景下都尽最大努力保障集群的高可用**。

#### 8.2.3 被动故障转移是有损的

即使在自动故障转移能够正常工作的场景下，节点退出后的恢复过程也不是无代价的。这个故障转移是在**毫无准备**的情况下被动发生的：

| 阶段 | 耗时 | 业务影响 |
|------|------|----------|
| 节点判死 | 数秒~数十秒 | 部分请求超时或失败 |
| 故障转移投票 | 数秒 | 相关分片不可写 |
| 新主节点就位 | 数秒 | 连接重建、缓存失效 |

在云环境中，一个热点实例的 QPS 可能高达**数十万甚至百万级别**。故障转移期间的每一秒，都可能意味着**数十万次请求失败**。对于金融交易、实时竞价、游戏等场景，这种不可预期的中断是难以接受的。

除了请求失败，还存在**数据丢失**的风险。由于 Redis/Valkey 采用异步复制，主节点突然退出时，尚未同步到副本的写入会随之永久丢失（详见 [3.3 被动故障转移的困境](#33-被动故障转移的困境)）。

#### 8.2.4 存量实例的挑战

新版本可以直接修改代码解决问题，但**老版本存量实例怎么办？**

在云上，这是一个巨大的挑战：
- 线上运行着**数以万计**的老版本实例
- 强制升级会影响业务，用户不一定配合
- 更棘手的是，在硬盘故障场景下，**升级过程本身就可能触发问题**——升级往往涉及节点重启或主从切换，这些操作都会引发集群元数据变更

#### 8.2.5 探索与实践

整个处理过程是一段**探索的旅程**。面对这个没有现成解决方案的难题，我们另辟蹊径：利用 Module 机制在运行时动态 Hook 函数，阻止进程退出，然后进行无缝迁移——**化被动为主动**。

坦白说，这不是一个优雅的解决方案。但它**有效**。

> **能解决问题是更重要的，业务可用性是最重要的。**

自这套方案上线以来，腾讯云 CRedis **再也没有出现过**因硬盘故障导致集群可用性问题的客户工单。这就是最好的证明。

#### 8.2.6 核心理念：化被动为主动

| 维度 | 被动故障转移 | 无缝迁移方案 |
|------|-------------|-------------|
| **触发时机** | 节点已经挂了 | 发现异常后主动介入处理 |
| **业务影响** | 不可控的中断 | 可控的、渐进式的迁移 |
| **处理窗口** | 没有（已经出问题了） | 充足（节点还在运行） |
| **运维姿态** | 救火 | 防火 |

这套方案的核心理念是：**不要等到房子着火了才去救火，而是提前做好防火措施**。即使火灾（硬盘故障）真的发生了，也能从容应对，而不是手忙脚乱。

### 8.3 版本建议

| 场景 | 建议 |
|------|------|
| **Valkey 用户** | 升级到 9.1 及以上版本，配置 `cluster-persist-config best-effort` |
| **Redis OSS 用户** | 关注社区是否有类似修复 |
| **云服务用户** | 确认云服务商是否有类似的防护措施 |
| **自建集群** | 参考本文的监控和应急预案 |

### 8.4 未来展望

这两个 PR 也引发了一些更深层次的思考，可能会在未来版本中得到解答：

- **无盘集群模式**：完全不依赖 `nodes.conf` 的集群模式
- **配置与状态分离**：将元数据拆分为不同文件，降低更新频率
- **异步写入**：将 `best-effort` 模式的保存操作改为在后台 I/O (bio) 线程中执行，避免 fsync 阻塞主线程（参见 PR #2555）
- **外部元数据存储**：支持将集群配置存储在 etcd 等外部系统

正如 zuiderkwast 在审查中所说：

> "Let's get this merged. It is sad that a fix like this can be stale for a very long time."
> 
> （让我们合并这个修复吧。一个这样的修复被搁置这么长时间，实在是令人遗憾。）

这句话不仅是对这个 PR 的感慨，也是对整个开源社区协作模式的反思。技术债务的偿还，往往需要漫长的讨论和权衡。

---

## 参考资料

- [Valkey PR #1032: Fail to save the cluster config file will not exit the process](https://github.com/valkey-io/valkey/pull/1032)
- [Valkey PR #3372: Add cluster-persist-config option to control nodes.conf save behavior](https://github.com/valkey-io/valkey/pull/3372)
- [Redis Cluster Specification](https://redis.io/docs/reference/cluster-spec/)
- [CLUSTER NODES Command Documentation](https://redis.io/docs/latest/commands/cluster-nodes/)
- [Valkey Official Documentation](https://valkey.io/docs/)

---

*本文基于 Valkey PR #1032 和 PR #3372 的公开讨论以及腾讯云 CRedis 团队的实践经验整理。感谢 enjoy-binbin、zuiderkwast、PingXie、madolson、hpatro、murphyjacob4 等社区贡献者的精彩讨论。*

---

**作者**：[Your Name]  
**日期**：2026 年 3 月  
**版权**：本文采用 CC BY-SA 4.0 协议发布

---

## 附录：关键代码位置

如果你想深入了解代码实现，可以参考以下文件：

| 文件 | 说明 |
|------|------|
| `src/server.h` | 定义 `cluster_persist_config_mode` 枚举和服务器状态 |
| `src/config.c` | 注册 `cluster-persist-config` 配置项 |
| `src/cluster_legacy.c` | 集群核心逻辑，包含 `clusterSaveConfigOrDie`、`clusterSaveConfigOrLog` 和 `clusterBeforeSleep` |
| `src/cluster.h` | 集群相关函数声明 |
| `valkey.conf` | 配置文件模板，包含 `cluster-persist-config` 的文档说明 |
| `tests/unit/cluster/misc.tcl` | 相关测试用例，测试 `sync` 和 `best-effort` 两种模式的行为 |
