# Default values for node-for-redis.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

clusterName: "valkey-cluster-test"

cert_source: "cert-manager"

resources: {}