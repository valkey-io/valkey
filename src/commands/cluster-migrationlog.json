{
    "MIGRATIONLOG": {
        "summary": "Get the history of previous slot import and export operations.",
        "complexity": "O(n), where N is the number of recent slot import and export jobs.",
        "group": "cluster",
        "since": "9.0.0",
        "container": "CLUSTER",
        "function": "clusterCommand",
        "arity": -2,
        "command_flags": [
            "NO_ASYNC_LOADING",
            "ADMIN",
            "STALE"
        ]
    }
}
